dependencies:
  post:
    - pip install -U pytest

test:
  override:
    - pytest test_circleci.py --junitxml=test.xml
  post:
    - mkdir $CIRCLE_ARTIFACTS/json_output
    - mv solo/target/*.xml $CIRCLE_ARTIFACTS/json_output
