dependencies:
  post:
    - pip install -U pytest

test:
  override:
    - pytest test_circleci.py --junitxml=test.xml
  post:
    - mkdir -p $CIRCLE_ARTIFACTS/junit/
    - find . -type f -regex ".*/target/surefire-reports/.*xml" -exec cp {} $CIRCLE_ARTIFACTS/junit/ \;
