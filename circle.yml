dependencies:
  post:
    - pip install -U pytest

test:
  override:
    - pytest test_circleci.py --junitxml=test1.xml
  post:
    - mkdir -p $CIRCLE_TEST_REPORTS/junit/
    - find . -type f -regex "./test1.xml"
    - cat test1.xml
