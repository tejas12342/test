test:

        override
                - pytest test_circleci.py --junitxml=test.xml 
                - mkdir -p $CIRCLE_TEST_REPORTS/junit
                - bundle exec junit --format junit --out $CIRCLE_TEST_REPORTS/junit/junit.xml
